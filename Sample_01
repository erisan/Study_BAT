#コメント
REM この行はコメントです
::ラベルを利用してこういう書き方もする

# バッチ変数の設定。型はなし
SET HENSU1=こんにちは世界

#バッチ変数を出力
ehco %HENSU1%

#ちなみに環境変数を見るときはSETを使うと表示できる
SET

#超わかりにくいforループ。DOの後に半角スペースが入るので注意
#基本構文
# for （オプション） %%アルファベット１文字 in (ループ処理の対象) do コマンド
# DIRのしたのファイルを一覧にする場合
for %%A in (DIR¥) DO (
 echo %%A
)
# ファイルの中身を読み込んで出力する場合
for /F %%A in (TARGET.txt) DO (
 echo %%A
)
# ファイルの中身はトークン(半角スペースなどでわけラれる)で分けて出力できるよ
# for /f "トークンオプション" %%i in (処理の対象) do コマンド
# 下の例の場合、1と3番めのトークンだけ取ってくる
for /f "tokens=1,3" %%A in (TARGET.txt) do (
echo %%A %%B
)
# トークンの区切り文字はオプションで指定できるよ。これだと　：が区切り文字としてあつかわれる
for /f "tokens=1,3 delims=:" %%A in (TARGET.txt) do (
echo %%A %%B
)
